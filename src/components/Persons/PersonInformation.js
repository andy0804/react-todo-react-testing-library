import React from 'react'

const PersonInformation = ({name}) => {
    return (
        <div>
            This is lazy loaded Person Information --- {name}
        </div>
    )
}

export default PersonInformation
